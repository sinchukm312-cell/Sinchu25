trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo "Hello, DevOps! Build started"
  displayName: 'Run a one-line script'

- script: |
    pip install -r requirements.txt
    python app.py
  displayName: 'Run Flask App'